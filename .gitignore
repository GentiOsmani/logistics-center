node_modules/
data/*.db
data/*.db-*
data/uploads/*
!data/uploads/.gitkeep
.env

# Static site build output — regenerated by .github/workflows/deploy.yml
dist/
# Generated seed SQL — contains a password hash, regenerate locally instead of committing
worker/seed.sql
# Wrangler's local cache (account id, auth state)
worker/.wrangler/
.wrangler/
